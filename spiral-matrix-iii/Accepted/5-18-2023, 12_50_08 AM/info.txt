{"id":952284000,"lang":"cpp","lang_name":"C++","time":"3 weeks, 1 day","timestamp":1684351208,"status":10,"status_display":"Accepted","runtime":"688 ms","url":"/submissions/detail/952284000/","is_pending":"Not Pending","title":"Spiral Matrix III","memory":"12.2 MB","code":"class Solution {\npublic:\n#define in_bound(rows, cols, rStart, cStart) ((rStart >= 0) && (rStart < rows) && (cStart >= 0) && (cStart < cols))\n\n    vector<vector<int>> spiralMatrixIII(int rows, int cols, int rStart, int cStart) {\n        vector<vector<int>> res;\n        int total_count = rows*cols;\n        int steps = 1;\n        //res.push_back({rStart,cStart});\n\n        while (rStart < total_count && cStart < total_count && rStart > -1*total_count && cStart > -1*total_count) {\n\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) res.push_back({rStart,cStart});\n                cStart++;\n            }\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) res.push_back({rStart,cStart});\n                rStart++;\n            }\n            steps++;\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) res.push_back({rStart,cStart});\n                cStart--;\n            }\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) res.push_back({rStart,cStart});\n                rStart--;\n            }\n            steps++;\n        }\n        return res;\n    }\n};","compare_result":"1111111111111111111111111111111111111111111111111111111111111111111111111","title_slug":"spiral-matrix-iii","has_notes":false}