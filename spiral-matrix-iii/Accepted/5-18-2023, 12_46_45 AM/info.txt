{"id":952282327,"lang":"cpp","lang_name":"C++","time":"3 weeks, 1 day","timestamp":1684351005,"status":10,"status_display":"Accepted","runtime":"681 ms","url":"/submissions/detail/952282327/","is_pending":"Not Pending","title":"Spiral Matrix III","memory":"12.2 MB","code":"class Solution {\npublic:\n    bool in_bound(int rows, int cols, int rStart, int cStart) {\n        return (rStart >= 0 && rStart < rows && cStart >= 0 && cStart < cols);\n    }\n    vector<vector<int>> spiralMatrixIII(int rows, int cols, int rStart, int cStart) {\n        vector<vector<int>> res;\n        int total_count = rows*cols;\n        int steps = 1;\n        //res.push_back({rStart,cStart});\n\n        while (rStart < total_count && cStart < total_count && rStart > -1*total_count && cStart > -1*total_count) {\n            // steps then turn left\n            // ++steps\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) {\n                    \n                    res.push_back({rStart,cStart});\n                }\n                //cout << rStart << \" \" << cStart << endl;\n                cStart++;\n            }\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) {\n                    \n                    res.push_back({rStart,cStart});\n                }\n                //cout << rStart << \" \" << cStart << endl;\n                rStart++;\n            }\n            steps++;\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) {\n                    \n                    res.push_back({rStart,cStart});\n                }\n                //cout << rStart << \" \" << cStart << endl;\n                cStart--;\n            }\n            for (int i = 0; i < steps; ++i) {\n                if (in_bound(rows,cols, rStart, cStart)) {\n                    \n                    res.push_back({rStart,cStart});\n                }\n                //cout << rStart << \" \" << cStart << endl;\n                rStart--;\n            }\n            steps++;\n        }\n        return res;\n    }\n};","compare_result":"1111111111111111111111111111111111111111111111111111111111111111111111111","title_slug":"spiral-matrix-iii","has_notes":false}